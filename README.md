# Authentication-System

This application is for practicing and learning the authentication process. I am building this application using mern stack.

for running the frontend use command:
      npm run client
      
for running backend use command:
      npm run server
